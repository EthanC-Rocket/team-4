# Requirements file for Sudoku Game
# This project uses only Python standard library, so no external dependencies are required.
# However, for testing and development, you may want to install:

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Code quality (optional)
# flake8>=6.0.0
# black>=23.0.0
